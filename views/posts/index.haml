.row
  .nine.columns.centered
    .posts
      %h1.gutter-bottom.title
        = link_to "&#xe04a;", "/", class: "no-underline icomoon"
        The Enquirer

      - if @posts.any?
        %ul
          - @posts.each do |p|
            %li
              = link_to p.title, "/blog/#{p.slug}"
      - else
        %p.mute.align-center.gutter-top-double.gutter-bottom-double
          %strong
            The Enquirer
          has no gossip on this subject.
          = link_to "Oh well, take me back.", "/"

      - if session[:admin]
        %footer.group
          %ul#admin.align-right
            %li= link_to "New Post", "/blog/new"
